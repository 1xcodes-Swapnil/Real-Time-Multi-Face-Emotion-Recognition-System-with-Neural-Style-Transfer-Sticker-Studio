# ─────────────────────────────────────────────────────────────────────────────
# MultiFace Emotion Recognition System — Requirements
# Python: 3.11.9 (download from https://www.python.org/ftp/python/3.11.9/python-3.11.9-amd64.exe)
#
# Install all at once:
#   py -3.11 -m pip install -r requirements.txt
#
# Install PyTorch GPU separately (RTX 3050 / CUDA 12.4):
#   py -3.11 -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# ─────────────────────────────────────────────────────────────────────────────

# ── Deep Learning (TensorFlow CPU — native Windows) ───────────────────────────
tensorflow==2.13.0          # 2.13 is latest that works on native Windows Python 3.11
keras>=2.13.0

# ── Computer Vision ───────────────────────────────────────────────────────────
opencv-python>=4.8.0        # webcam, face detection, image processing

# ── PyTorch (AnimeGAN2 style transfer) ───────────────────────────────────────
# Install separately with CUDA support:
# py -3.11 -m pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
torch>=2.0.0
torchvision>=0.15.0

# ── UI Framework ─────────────────────────────────────────────────────────────
# Tkinter is built into Python — no install needed

# ── MediaPipe (facial landmarks for emoji) ───────────────────────────────────
mediapipe>=0.10.0

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=9.0.0               # GIF frame extraction, image handling
numpy>=1.23.0

# ── HTTP / API ────────────────────────────────────────────────────────────────
requests>=2.28.0            # Giphy API calls

# ── ML utilities ──────────────────────────────────────────────────────────────
scikit-learn>=1.2.0         # compute_class_weight, confusion matrix

# ── Training monitoring ───────────────────────────────────────────────────────
tensorboard>=2.13.0